# everything-ai
Complete modular self-contained AI framework built entirely inside the Grok sandbox: core neural nets, subsumption agents, multi-agent blackboard, vector memory, tool registry. Offline-first, pure Python + PyTorch.
